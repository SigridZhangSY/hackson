import React from 'react';
import '../../stylesheets/indexContent.scss'

const ClusterTab = ({children}) => (
    <div className="cluster-tab">
        {children}
    </div>
);

export default ClusterTab;